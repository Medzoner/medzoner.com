imports:
    # Framework - framework
    - { resource: framework/framework.yml }
    # Framework - twig
    - { resource: framework/twig.yml }
    # Framework - assetic
    - { resource: framework/assetic.yml }
    # Framework - doctrine
    - { resource: framework/doctrine.yml }
    # Framework - swiftmailer
    - { resource: framework/swiftmailer.yml }

    # Parameters
    - { resource: parameters.yml }
    # Security
    - { resource: security.yml }

    # Bundles - Fos Elastica
    #- { resource: bundles/fos_elastica.yml }
    # Bundles - Fos User
    - { resource: bundles/fos_user.yml }
    # Bundles - Knp Menu
    - { resource: bundles/knp_menu.yml }
    # Bundles - Liip Imagine
    - { resource: bundles/liip_imagine.yml }


monolog:
  handlers:
      main:
          type:         fingers_crossed
          action_level: critical
          handler:      grouped
      grouped:
          type:    group
          members: [streamed, buffered]
      streamed:
          type:  stream
          path:  "%kernel.logs_dir%/%kernel.environment%.log"
          level: debug
      buffered:
          type:    buffer
          handler: swift
      swift:
          type:       swift_mailer
          from_email: %monolog_mail%
          to_email:   %monolog_mail%
          subject:    "[medzoner.com] An Error Occurred!"
          level:      debug