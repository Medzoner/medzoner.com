{# src/Site/BlogBundle/Resources/public/views/Comment/form.html.twig #}
{% if is_granted("ROLE_USER") %}
<form action="{{ path('site_blog_comment_create', { 'blog_id' : comment.blog.id } ) }}" method="post" {{ form_enctype(form) }} class="blogger">
    {{ form_widget(form) }}
    <p>
        <input type="submit" value="Submit">
    </p>
</form>
{% else %}
<p> <a href="/login" />Connexion requise</p>
{% endif %}