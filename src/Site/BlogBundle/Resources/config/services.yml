parameters:
#    site_blog.example.class: Site\BlogBundle\Example

services:
#    site_blog.example:
#        class: %site_blog.example.class%
#        arguments: [@service_id, "plain_value", %parameter%]

    blogger_blog.twig.extension:
        class: Site\BlogBundle\Twig\Extensions\SiteBlogExtension
        tags:
            - { name: twig.extension }

    wsse.security.authentication.provider:
      class:  Site\BlogBundle\Security\Authentication\Provider\WsseProvider
      arguments: ['', %kernel.cache_dir%/security/nonces]

    wsse.security.authentication.listener:
      class:  Site\BlogBundle\Security\Firewall\WsseListener
      arguments: ["@security.context", "@security.authentication.manager"]