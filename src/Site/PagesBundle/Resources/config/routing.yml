site_foundation:
    path:  /foundation
    defaults: { _controller: SitePagesBundle:Foundation:index }
    
site_pages_proxy:
    path:  /proxy
    defaults: { _controller: SitePagesBundle:Proxy:index }
    
site_pages_proxy_url:
    path:  /proxy/{url}
    defaults: { _controller: SitePagesBundle:Proxy:index }

#site_search:
#    path: /search
#    defaults: { _controller: SitePagesBundle:Search:index }