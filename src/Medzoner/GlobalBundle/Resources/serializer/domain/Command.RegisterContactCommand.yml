Medzoner\Domain\Command\RegisterContactCommand:
    exclusion_policy: 'ALL'

    properties:
        name:
            expose: true
            type: 'string'

        email:
            expose: true
            type: 'string'

        message:
            expose: true
            type: 'string'

        dateAdd:
            expose: true
            type: 'DateTime'
