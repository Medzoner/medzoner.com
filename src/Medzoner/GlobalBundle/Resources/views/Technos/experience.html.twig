<div class="cv-tree">
    <div class="tree">
        <ul>
            <li><h3> Experience</h3>
                <ul>
                    {% for k,experience in experiences %}

                        <li><span class="folder-title"> {{ k }}</span>
                            <ul>
                                {% for item in experience %}
                                    {% if item.title is defined %}
                                        <li>{{ item.title }}</li>
                                    {% endif %}

                                    {% if item.title is not defined %}
                                        {% for k,subexperience in item %}
                                            <li><span class="folder-title"> {{ k }}</span>
                                                <ul>
                                                    {% for subitem in subexperience %}
                                                        <li>{{ subitem.title }}</li>

                                                    {% endfor %}
                                                </ul>
                                            </li>
                                        {% endfor %}
                                    {% endif %}

                                {% endfor %}
                            </ul>
                        </li>

                    {% endfor %}
                </ul>
            </li>
        </ul>
    </div>
</div>
