    <section class="large-12 columns">
        <header>
        <nav class="top-bar" data-topbar="">
            <ul class="title-area">
                <li class="name">
                    <a href="{{ path('site') }}"><img src="{{ asset(medzoner_cdn_url ~ '/images/logo.png') }}" alt="WWW.MEDZONER.COM"/></a>
                </li>
                <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
            </ul>

            <section class="top-bar-section">
                <ul>
                    <li class="first">
                        <a href="{{ path('site_cv') }}">CV</a>
                    </li>
                    <li class="last">
                        <a href="{{ path('site_contact') }}">CONTACT</a>
                    </li>
                </ul>
            </section>

            {% if is_granted("ROLE_ADMIN") %}
                <section class="top-bar-section">
                    <ul class="left">
                        <li><p>Administration : </p></li>
                        <li>
                            <a href="{{ path('site_blog_new') }}">{{ 'new post' | trans }}</a>
                        </li>
                    </ul>
                </section>
            {% endif %}
        </nav>
        </header>
    </section>
